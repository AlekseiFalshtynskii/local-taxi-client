export class Car {
  model: string;
  regNumber: string;
  color: string;

  constructor(model: string, regNumber: string, color: string) {
    this.model = model;
    this.regNumber = regNumber;
    this.color = color;
  }
}
